@model IEnumerable<HireGnome.Models.Products>
@{
    ViewBag.Title = "Search";
}

<h2>Search</h2>

@if (Model.Count() > 0)
{
    <ul>
        @foreach (var gnome in Model)
        {
            <li>@gnome.Name</li>
        }
    </ul>
}
else
{
    <b>No results</b>
}


